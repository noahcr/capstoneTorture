var zip = parsFloat(zipCode);
function populatehtml(locOperation){
   document.getElementById('zipCode')
   if endsWith('stringOne')
   // need fnct for returns w/ location user should go to based on string
   var location = locOperation();

document.getElementById('closestLocation').innerText += location
}

document.getElementById('calc').onclick = function(){
   populatehtml(operation);

 
 function populatehtml(waitOperation){
   document.getElementById('zipCode')
   if endsWith('stringOne')
      // need fnct that retrns w/ wait time based on location
   var wait= waitOperation();

document.getElementById('waitTime').innerText += wait
 }